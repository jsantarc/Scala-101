Please pay special attention to sections term and type Compaction objects and case classes  andOptions Quiz questions Module 21)    Consider the following code segment:class YoYo(message: String  ){print("Hello")}new YoYo("YoYo")what would be the output: a)    Hello*b)    YoYoc)    error: not enough arguments for constructor YoYo: d)    7e)    H2)    Consider the following code segment:class YoYo(message: String  ){}(new YoYo("YoYo")).messagewhat would be the output: a)  "YoYo"b) error: value message is not a member of YoYo*3)    c) hello 3) Consider the following code segment:class YoYo(val message: String  ){}(new YoYo("YoYo")).messagewhat would be the output: a)  "YoYo"*b) error: value message is not a member of YoYoc) hello 4)    Consider the following code segmentclass YoYo(val message: String  ){}(new YoYo("YoYo")).messagea)  "YoYo"b) error: value message is not a member of YoYo*c) hello 5) What is the  key difference between fields and methods a) fields are privateb) methods are private c)   You can change the value of a method after the object has been created*d) Methods are private  Module 3 1)    In  a case class, the following is incorrecta)    You need the new keyword when creating an instance*b)    parameters are immutable fields c)    they are less complicated to create then regular classes d)    they have useful prebuild methods 2)    what is the result of the following operation of the two instances of the case class time Time(9,10)==Time(9,10)a)    false* b)    truec)    error d)    (9,10)3)    What is the result of the following operations on the case class Time   Time(8,40).hashCode()==Time(8,41).hashCode()a)    false *b)    truec)    error d)    -12334344) what can you say about the following two operations in the case class Time:Time.apply(1,2)  and Time (1,2)a)     application  is only for regular class b)    apply will create several Synthetic Methodsc)    They are identical *d)    apply() is for  immutable  objects 5)    If two instances of the same case class have identical parameters the method "hashCode()" will output the same response.a) True*b) FalseModule 41)  What fact is true about arrays a)values are contiguous in memory*b)  have a linked list implementation where values point to the next c) Theoretically limitless in size  </li>d) Poor lookup performance as data could be located anywhere in memory   2)  Is the following sequence of  numbers and a set :{1,2,2,2,2,3,4}a) True b) False*3)  What is the advantage  of "getOrElse" in maps a) the lookup time is faster b) if the key does not exist it can output can be predefined*c) its zero-based d) it can be used in map reduced 4)  consider the range object "Range" what is the output of the following Range.map(n=>n)a) it will add one to every element in the variable range b) it will multiply every number by 2c) the values will not changed) we will get an error 5) what is the result of the following operation:List(1,1,1).product a) 10b) 1c) 100d) 11